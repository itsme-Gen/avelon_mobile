import { View,Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

export default function WalletScreen() {
    return(
        <SafeAreaView>
            <View>
                <Text>Wallet Screen goes here</Text>
            </View>
        </SafeAreaView>
    )
}